# Cloudflare Deploy Starter

This repository is configured for `npx wrangler deploy` with static assets.

## Structure

- `public/`: static files to publish
- `wrangler.toml`: Wrangler deployment config

## Deploy

```bash
npx wrangler deploy
```